// Default settings of changeable aspects of the UI
const JQUERY_SELECTOR_ERROR_ENABLED = true;

// Transition options
const SHOW_OPTIONS = {
    easing: 'swing',
    duration: 600,
};
const HIDE_OPTIONS = {
    easing: 'swing',
    duration: 300,
};
