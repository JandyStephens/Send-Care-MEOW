// Ajax Query Templates
const twitterQueryURL = (whatever, it, needs) => `http://whatever/?here=${whatever}&we=${it}&go=${needs}`;
const postmatesQueryURL = (whatever, it, needs) => `http://whatever/?here=${whatever}&we=${it}&go=${needs}`;